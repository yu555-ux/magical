---
变量更新规则:
  世界:
    梦境定位.即将入睡:
      type: boolean
      check:
        - 深夜/午休/昏迷等入睡情境设为true，成功入梦或清醒后设为false

    梦境定位.位于梦境:
      type: boolean
      check:
        - 进入梦境世界设为true，正常苏醒/梦境中死亡强制退出时设为false

    现实.时间:
      format: YYYY年MM月DD日-星期X-HH:MM
      check:
        - 随剧情推进、场景切换自然流逝；普通对话+5~15分钟，战斗/探索+30分钟~2小时，旅行/长休+数小时；跨日时星期同步更新

    现实.地点:
      check:
        - 随主角在现实中的移动更新，只保留最后两个路径层级（如"11号楼-601室"）

    现实.天气:
      check:
        - 随时间推移或场景氛围需求变更；极端天气应配合剧情节奏，不宜频繁突变

    梦境存档.时间:
      format: YYYY年MM月DD日-星期X-HH:MM
      check:
        - 仅在"位于梦境"为true时与剧情同步流逝

    梦境存档.地点:
      check:
        - 随主角在梦境中的移动更新，同样只保留最后两个路径层级

    梦境存档.天气:
      check:
        - 梦境中始终为"残月"，不更新

    倒计时.可进入梦境倒计时:
      format: HH:MM
      check:
        - 从梦境苏醒后重置为"12:00"并开始倒计时；在梦境中恒为"00:00"；归零后才可再次进入梦境

    倒计时.离开梦境倒计时:
      format: HH:MM
      check:
        - 进入梦境后重置为"24:00"并开始倒计时；归零时强制苏醒回到现实

  主角:
    年龄:
      type: number
      check:
        - 跟随世界时间，每过一年+1

    资源.超凡资源.${蝶烬|尸气}:
      type: number
      check:
        - 蝶烬：仅在梦境击杀红蝶环绕的异常/收集掉落物时增加，具现物品/使用梦境化身时扣除
        - 尸气：现实和梦境击杀厉鬼僵尸类异常时增加，与柳三娘交易时扣除

    资源.金钱.数值:
      type: number
      check:
        - 获得收入或消费时增减；普通日常消费百元级

    性器.${疲软长度|勃起长度}:
      type: number
      check:
        - 一般情况不更新，仅特殊超凡改造时调整

    评级:
      value: 微尘 → 聚砂 → 凝石 → 凶煞 → 祸城 → 覆国 → 灭世（由低到高，不可逆）
      check:
        - 实力产生质的飞跃或通过鉴灵碑测试后升级

    身体属性.${生命|体力|能量|SAN}.当前:
      type: number
      range: 0~上限
      check:
        - 生命：受伤扣减，休息/治疗恢复
        - 体力：奔跑/战斗/重体力劳动消耗，休息/进食恢复
        - 能量：使用技能消耗，休息/冥想恢复
        - SAN：直视不可名状/恐惧事件下降，安全休息/心理慰藉恢复

    身体属性.${生命|体力|能量|SAN}.上限:
      type: number
      check:
        - 随评级提升或奇物强化增加；一般与评级联动，不单独频繁更新

    基础属性.${力量|体质|精神|敏捷}:
      type: number
      range: 1~100
      check:
        - 实战磨炼、境界突破或奇物强化时增加，单次≤±5

    特殊属性.${幸运|魅力}:
      type: number
      range: 1~100
      check:
        - 极难提升，仅极特殊奇物或重大命运变故时微调，单次≤±3

    状态:
      type: |-
        {
          [状态名: string]: {
            描述: string;
            持续时间?: string;
          }
        }
      check:
        - 获得新buff/debuff/异常状态时新增条目；状态过期或消失时删除对应条目

    技能.${技能名}:
      type: |-
        {
          等级: '微尘' | '聚砂' | '凝石' | '凶煞' | '祸城' | '覆国' | '灭世';
          描述: string;
          使用要求: string;
          副作用: string;
          熟练度: number;
          分支: { [分支名: string]: { 描述: string; 效果: string; } };
        }
      check:
        - 学习新技能时新增条目
        - 熟练度：成功使用技能后+(1~20)，长期不使用缓慢衰减
        - 等级：熟练度达标且技能有质的突破时升级，不可逆
        - 分支：熟练度达到阈值后解锁对应分支条目

    持有物品.灵宝:
      type: |-
        {
          [灵宝名: string]: {
            等级: '微末' | '凶煞' | '祸城' | '倾国' | '绝域' | '灭世';
            描述: string;
            数量: number;
            效果: { [效果名: string]: string; };
          }
        }
      check:
        - 获得新灵宝时新增，遗失/转赠/毁坏时删除条目或减少数量

    持有物品.诡物:
      type: |-
        {
          [诡物名: string]: {
            等级: '微末' | '凶煞' | '祸城' | '倾国' | '绝域' | '灭世';
            描述: string;
            数量: number;
            规则: { [规则名: string]: string; };
            副作用: { [副作用名: string]: string; };
          }
        }
      check:
        - 获得新诡物时新增，必须严格记录触发规则与副作用

    持有物品.物品:
      type: |-
        {
          [物品名: string]: { 数量: number; 描述: string; }
        }
      check:
        - 获得普通物品/材料时新增或增加数量，消耗时扣除

    社交.${角色名}.关系:
      check:
        - 人际关系发生本质变化时更新（如朋友→恋人、陌生人→仇敌）；措辞简洁

  仓库:
    ${灵宝|诡物|物品}:
      check:
        - 结构与主角持有物品对应类别相同；主角存取物品时由系统代码更新，AI 不需要手动操作

  主要人物:
    女性.${异人|普通人}.${角色名}:
      好感值:
        type: number
        range: -200~200
        category:
          -200~-100: 仇敌
          -99~-1: 反感
          0~50: 陌生人
          51~100: 熟人
          101~150: 好友
          151~200: 挚爱
        check:
          - 根据该女性对主角言行的感受和互动亲密度增减，单次±(1~10)
          - 伤害/背叛大减(-20~-50)，牺牲/拯救大加(+20~+50)
          - 长期不互动缓慢衰减(-1~-3每场景)
          - 前端自动将数值映射为$好感阶段，AI 只更新数值

      堕落值:
        type: number
        range: 0~500
        check:
          - 经历背德行为、非自愿/乱交情境中获得快感时增加，单次+(1~5)
          - 长期无亲密互动缓慢下降(-1~-2每场景)
          - 前端自动将数值映射为$堕落阶段，AI 只更新数值

      性欲值:
        type: number
        range: 0~100
        check:
          - 受到性刺激/挑逗/色情内容影响时上升，单次+(1~10)
          - 长时间无刺激或性释放后自然缓慢下降(-1~-2每场景)
          - 随生理周期波动，排卵期更容易上升

      当前位置:
        check:
          - 随角色物理移动更新，同样只保留最后两个路径层级

      当前行动:
        check:
          - 每个叙事回合更新，描述角色当下正在做的具体行为或姿势

      当前想法:
        check:
          - 以第一人称简短记录角色当下的心理活动，15字左右

      状态:
        type: |-
          {
            [状态名: string]: {
              描述: string;
              持续时间?: string;
            }
          }
        check:
          - 获得新buff/debuff/异常状态时新增条目；状态过期或消失时删除对应条目

      身体属性.${生命|SAN}.${当前|上限}:
        type: number
        check:
          - 当前：受伤扣减，休息恢复；上限：随角色成长提升
          - 异人额外包含能量字段，更新逻辑同生命

      基础属性.${力量|体质|精神|敏捷}:
        type: number
        check:
          - 随角色成长变化，单次≤±5

      特殊属性.${幸运|魅力}:
        type: number
        check:
          - 极难提升，仅重大变故或奇物影响时微调

      着装.${上衣|下衣|内衣|袜子|鞋子}:
        type: |-
          { 名称: string; 描述: string; }
        check:
          - 更换衣物/战斗破损/亲密行为脱下时更新

      身体开发.${嘴巴|胸部|小穴|屁穴}.使用次数:
        type: number
        check:
          - 发生亲密接触或性行为时对应部位+1
          - 前端自动映射为$开发阶段，AI 只更新数值

      身体开发.${嘴巴|胸部|小穴|屁穴}.描述:
        check:
          - 随开发程度逐步展现敏感度、肉体形状改变和适应性反应

    男性.${异人|普通人}.${角色名}:
      友善值:
        type: number
        range: -200~200
        category:
          -200~-100: 敌对
          -99~-1: 冷淡
          0~50: 陌生人
          51~100: 相识
          101~150: 友好
          151~200: 挚友
        check:
          - 根据该男性对主角行为的认可度和利益交集增减
          - 增减幅度及衰减规则同女性好感值
          - 前端自动将数值映射为$友善阶段，AI 只更新数值

      当前位置:
        check:
          - 随角色物理移动更新，同样只保留最后两个路径层级

      当前行动:
        check:
          - 每个叙事回合更新，描述角色当下正在做的具体行为

      当前想法:
        check:
          - 以第一人称简短记录角色当下的心理活动，15字左右

      状态:
        type: |-
          {
            [状态名: string]: {
              描述: string;
              持续时间?: string;
            }
          }
        check:
          - 同女性角色状态规则

      身体属性.${生命|SAN}.${当前|上限}:
        type: number
        check:
          - 当前：受伤扣减，休息恢复；上限：随角色成长提升
          - 异人额外包含能量字段，更新逻辑同生命

      基础属性.${力量|体质|精神|敏捷}:
        type: number
        check:
          - 随角色成长变化，单次≤±5

      特殊属性.${幸运|魅力}:
        type: number
        check:
          - 极难提升，仅重大变故或奇物影响时微调

    ${女性|男性}.异人.${角色名}:
      评级:
        value: 微尘 → 聚砂 → 凝石 → 凶煞 → 祸城 → 覆国 → 灭世（由低到高，不可逆）
        check:
          - 发生境界突破或展示隐藏实力时更新

      技能:
        check:
          - 结构同主角技能，展现该角色的超凡能力与分支

      所持物品.${灵宝|诡物|物品}:
        check:
          - 结构同主角持有物品对应类别，记录角色随身携带的超凡物品

    女性.${异人|普通人}.${角色名}.子宫.生理周期.周期天数:
      type: number
      check:
        - 新角色首次登场时设定，通常为28天；仅需设置一次
    女性.${异人|普通人}.${角色名}.子宫.生理周期.经期长度:
      type: number
      check:
        - 新角色首次登场时设定，通常为3~7天；仅需设置一次
    女性.${异人|普通人}.${角色名}.子宫.生理周期.上次经期日:
      format: YYYY年MM月DD日
      check:
        - 新角色首次登场时设定，选取剧情开始前的一个合理日期；仅需设置一次

  地图:
    ${任意地点}.${现实|梦境}.描述:
      check:
        - 主角初次抵达该地点、或环境发生显著变化时更新物理/氛围描述
    ${任意地点}.${现实|梦境}.地点细节.信息:
      type: string[]
      check:
        - 探索中收集到与该地相关的线索/情报时追加条目
    ${任意地点}.现实.地点细节.异常.${异常名}:
      check:
        - 在现实中发现超凡痕迹或异常传说时新增条目，记录评级、描述
    ${任意地点}.梦境.地点细节.异常.${异常名}:
      check:
        - 在梦境中遭遇新异常存在时新增条目，必须包含评级、描述、具现进度、特性
    ${任意地点}.梦境.地点细节.异常.${异常名}.具现进度:
      type: number
      range: 0~100
      check:
        - 异常周围紫蝶越多具现进度越大；进度达100表示异常完全具现；被削弱/驱散时降低
    ${任意地点}.子地图.${新地点名}:
      check:
        - 探索发现新区域时，在对应父地点下嵌套新增子地图节点
        - 新地点需包含检索词、方位、现实/梦境描述及基础的地点细节结构
