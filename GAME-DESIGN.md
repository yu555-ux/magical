# Game Design Document — 梦境与魔法少女

> 本文档描述核心设计原则和待实现功能。已实现功能的详细方案见 `docs/` 目录。

---

## 一、位置系统设计

### 1.1 核心思想

**字符串即键名。** `当前位置` 保持为纯字符串，不做结构改动。需要路径/坐标时，通过检索词匹配地图树实时推导，不冗余存储。

### 1.2 变量格式

所有位置字段存储为**两级拼接字符串** `父节点-当前节点`：

```
世界.现实.地点: '11号楼-601室'
张云.当前位置: '11号楼-601室'
顾昀.当前位置: '11号楼-602室'
周安.当前位置: '东煌区-江城'
```

路径只有一级时直接存节点名。AI 输出的单节点名由 `formatLocation()` 自动拼接为两级格式后存储。

### 1.3 AI 输出格式

AI 只输出叶子节点名，不输出完整路径。系统负责路径解析和拼接：

```xml
<vars>
主角:
  当前位置: 601室
顾昀:
  当前位置: 602室
</vars>
```

### 1.4 已实现功能

| 功能 | 方案文档 |
|------|---------|
| 地图变量注入（`{{MAP}}` 宏） | `docs/地图变量注入系统.md` |
| 人物变量注入（`{{FEMALE_STRANGER}}` 等四个宏） | `docs/人物变量注入系统.md` |
| 位置解析、拆分匹配、自动拼接 | `docs/地图变量注入系统.md` §三 |

### 1.5 坐标补充判定（待实现）

当路径不精确时，用坐标做范围兜底：

- 每个地图节点已有 `方位: { X:[min,max], Y:[min,max], Z:[min,max] }`
- 如果目标坐标落在当前区域的方位范围内 → 确认同区域
- 坐标比较是纯数值运算，确定性极高

---

## 二、地图数据结构

### 2.1 节点标准格式

```
节点名: {
  检索词: ['关键词1', '关键词2'],
  方位: { X: [min, max], Y: [min, max], Z: [min, max] },
  现实: {
    描述: '...',
    地点细节: { 信息: [], 异常: {} }
  },
  梦境: {
    描述: '...',
    地点细节: { 信息: [], 异常: {} }
  },
  子地图: { ... }
}
```

### 2.2 地图树示例

```
蓝星
├── 东煌区
│   ├── 夏城
│   │   ├── 锦荣区
│   │   │   ├── 幸福小区
│   │   │   │   ├── 小区门卫室
│   │   │   │   ├── 中心广场
│   │   │   │   ├── 地下车库
│   │   │   │   ├── 浅阳湖
│   │   │   │   └── 11号楼
│   │   │   │       ├── 102室
│   │   │   │       ├── 202室
│   │   │   │       ├── 301室（柳三娘家）
│   │   │   │       ├── 601室（玩家家）
│   │   │   │       └── 602室（顾昀家）
│   │   │   ├── 夏城一中
│   │   │   ├── 夏城三中
│   │   │   ├── 夏城第一医院
│   │   │   └── 大学城
│   │   │       └── 夏城师范大学
│   ├── 京都
│   └── 江城
└── 异界
    检索词: ['异界', '异世界']
    子地图: {}
```

---

## 三、玩法扩展建议

### 优先级 1：低实现成本，高乐趣

#### 3.1 世界地点自动跟随
- 主角位置变更 → `世界.现实.地点` 同步
- 或反过来：世界地点变更 → 主角位置同步（表示"故事场景切到哪"）

#### 3.2 移动距离/时间估算
- 地图节点已有 `方位` 范围 → 两个位置各取中点 → 直线距离
- 注入 prompt 辅助 AI 判断：`家 → 夏城一中 约3km，步行30分钟`

### 优先级 2：中等成本，高沉浸感

#### 3.3 移动触发时间推进
- 检测到路径变更 → 自动推进 `世界.现实.时间`
- 聊天+5min，短距离移动+15min，远距离移动+数小时

#### 3.4 时间感知的 NPC 行为
- 地图节点可扩充 `NPC活动` 字段
- 主角到某处时，系统自动把同时间应在该处的 NPC 位置切过去

### 优先级 3：高成本，定义游戏类型

#### 3.5 随机遭遇系统
- 结合时间+位置+概率的事件池
- 路上遇到 NPC、突发袭击、神秘电话

#### 3.6 双世界切换
- 现实↔梦境，同一地图节点不同坐标/描述
- 梦境中可使用超凡能力
